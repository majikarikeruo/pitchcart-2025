---

指摘事項 ファクトチェック結果
#: 1  
 指摘内容: API認証がコメントアウト - 全分析エンドポイントが無認証  
 判定: TRUE
根拠: 4箇所(/analyze/emotional_arc, /simulate/structure, /analyze/stream, /analyze)すべてで SKIPPING AUTH + verifyRequest がコメントアウト済み
────────────────────────────────────────
#: 2
指摘内容: Mastraワークフローがplaceholder - LLM直呼びフォールバックのみ
判定: TRUE
根拠: mastra.ts のコメントに「placeholder and high-level orchestrator」と明記。2関数とも中身は llmEvaluatePersonaWithOpts / llmMergeConsensusWithOpts をそのまま呼ぶだけ
────────────────────────────────────────
#: 3
指摘内容: 拡張分析がプロンプト生成のみ - AI分析に未接続
判定: TRUE
根拠: useEnhancedAnalysis.ts:54 で result = { prompt, timestamp } を返すだけ。コメントにも「実際の分析は既存のフローに任せる」とあり、AI APIの呼び出しなし。利用箇所も AnalysisInsights.tsx と
features/Entry/PresentationCheck.tsx のみで本流とは別
────────────────────────────────────────
#: 4
指摘内容: ダッシュボード「分析詳細」タブが"開発中..."
判定: TRUE
根拠: Dashboard.tsx:212,222 に 開発中... のテキスト表示のみ
────────────────────────────────────────
#: 5
指摘内容: プレゼンカレンダーがサンプル固定データ・未使用
判定: TRUE
根拠: sampleEvents ハードコード、追加ボタンにonClickハンドラなし。コンポーネントを import しているファイルが定義ファイル自身以外にゼロ
────────────────────────────────────────
#: 6
指摘内容: 目標管理がローカルstateのみ・永続化なし
判定: TRUE
根拠: useState<Goal[]>(sampleGoals) のみ。Firestore等への保存/読込のコードは一切なし。ページリロードで全消失
────────────────────────────────────────
#: 7
指摘内容: スライド画像がサンプル固定 - PPTX実画像連携なし
判定: TRUE
根拠: SlideNode.tsx:71 と SlideImage.tsx:11 ともに /assets/images/sample${XX}.jpg をハードコード。アップロードされたPPTXから画像を抽出・表示する処理なし
────────────────────────────────────────
#: 8
指摘内容: スコアカテゴリ計算が仮マッピング - technicalQuality固定50
判定: TRUE
根拠: analysis.service.ts:244 にコメント「仮」、249行目 design: categoryScores.uniqueness は意味的に不一致、251行目 technicalQuality: 50 が完全ハードコード
────────────────────────────────────────
#: 9
指摘内容: ダミーデータ生成サービスが未配線
判定: TRUE
根拠: dummy.service.ts をインポートしているファイルがゼロ。定義のみ存在
────────────────────────────────────────
#: 10
指摘内容: 旧APIフロー用エントリコンポーネントが未使用
判定: 一部TRUE
根拠: src/components/Entry/PresentationCheck.tsx は確かにどこからもインポートされていない (旧パス)。ただし現行の Entry.tsx は src/components/features/Entry/PresentationCheck.tsx (新パス)
を使っており、こちらは使用中。旧パスの方だけが死んでいる

---
